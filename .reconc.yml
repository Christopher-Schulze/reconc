# Generated by `reconc init`.
#
# `extends:` merges rules from bundled or user preset packs into
# this repo's compiled lockfile. Run `reconc preset list` to see
# every available pack and `reconc preset show NAME` to inspect one.
extends:
  - default
  - agent
  - go-assurance
  - docs-sync
  - release
  - strict
task_lifecycle:
  profile: sections-v1
  overview_path: docs/tasks.md
  detail_dir: docs/tasks
  done_dir: docs/tasks/done
  done_visible: 10
  completion:
    require_committed: true
# Add repo-local rules below. They compose with the preset rules
# listed above; duplicate rule IDs are a hard error.
rules: []
