#!/bin/sh
# Root-safe task-claim launcher for the nested template harness Go module.
set -eu

script_dir="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
harness_dir="$(CDPATH= cd -- "$script_dir/../.." && pwd)"

cd "$harness_dir"
exec go run ./utils/task-claim "$@"
