# TASK 545: Bind live hook proof to host execution

## Why

Current live verification is operator-driven, lacks executable/version binding, and can infer enforcement from one blocked record plus two absent markers. That does not prove that both operations were attempted and denied by the actual host.

## Acceptance

- Every live result identifies the resolved host executable, reported version, binary digest, surface/mode, Reconc build and adapter digest, probe/run identity, repository/config identity, and time.
- Each enforcement claim has its own attempted operation, decision, observed host outcome, and independent side-effect check. Missing events, unsupported routes, authentication failures, timeouts, and skips cannot become pass.
- A wrong executable named `agent` is rejected as Cursor. All five requested hosts have explicit discovery and version-reporting contracts.
- Synthetic adapter tests remain visibly distinct from real host qualification. Version or configuration changes invalidate reuse of the old qualification.
- The existing CLI and probe script expose bounded, machine-readable verification without changing ordinary user sessions or the product repository.

## Sub-Tasks

- [x] Define the typed receipt and per-operation evidence model against the current verifier and schemas.
- [x] Implement exact executable discovery, version qualification, and causal probe accounting.
- [x] Add bounded host runners or explicit operator-assisted steps where no supported unattended interface exists.
- [x] Exercise positive and negative controls and propagate status, diagnostics, schema, and documentation.

## Technical Plan

1. Extend `internal/cli/hook_verify_cmd.go`, `hook_verify_live.go`, `internal/hooks/verification.go`, and their existing tests. Reuse `scripts/tests/host-integration-probe.sh`; do not build a second verification framework.
2. Replace availability-only PATH checks with host-specific identification. Prefer `cursor-agent`; accept `agent` only after positive Cursor identity verification. Add Codex and Devin, which currently reach the unchecked default, and DSH once TASK 550 provides the platform. Resolve symlinks and record the executable actually launched.
3. Track read, allowed write, denied write, allowed shell, denied shell, nonzero shell, and classified MCP operations independently. Give each operation a fresh nonce and distinct marker. A successful allowed operation is the positive control that the host and probe actually ran. A missing marker alone proves nothing.
4. Require matching pre-decision and host-attempt/result evidence for each denied operation. Where the host omits a result after denial, use its explicit rejection plus the linked attempted call. Never equate operator Enter, route liveness, or a synthetic invocation with native enforcement.
5. Keep current isolated-workspace creation and output limits. Add an overall deadline, per-action deadlines, bounded retries only for diagnosed transient setup errors, cancellation of child processes, and redacted bounded logs. Do not change authentication configuration or attach to a user's active conversation.
6. Use documented headless, SDK, or interactive interfaces only after reading their exact current flags. Preserve the explicit `--allow-authenticated` contract. JSON/noninteractive invocation must never wait indefinitely for Enter. Operator-assisted results must identify that mode.
7. Extend schemas through their current registry owner and regenerate affected reference docs. Reuse existing verification/status fields where compatible; introduce a format revision only if required by the schema's compatibility policy, without changing immutable historical schema identities.

## Verification

Run `go test ./internal/cli ./internal/hooks ./internal/schema` with focused regressions first. Include the wrong-host alias, zero attempted operations, only one of two deny probes, stale receipts, mismatched repository, changed config, duplicate/replayed IDs, canceled host, malformed output, and missing authentication. Real-host qualification is owned by TASK 555 after the adapters are complete.

## Dependencies

TASK 544 for reliable closure gates. TASKS 546-550 supply host-specific runners and fixtures; TASK 545 defines their shared contract first.

## Notes

At the planning baseline, PATH resolves `agent` to `~/.grok/bin/agent`; the actual Cursor executable is `~/.local/bin/cursor-agent`. Current `applyLiveHookProbeRecords` derives enforcement from `summary.blocked` and both markers being absent. Local installed versions: Codex `0.154.0`, Devin `3000.10.21 (611c1cba)`, Cursor `2026.09.10-fd3934a`, OMP `18.1.18`. DSH was not found on PATH. These observations are inventory, not live enforcement evidence.

Execution-entry review also confirms that `initializeHookVerificationRepo` currently creates only a deny_write rule for forbidden.txt, not a policy against the separate forbidden-command-marker. The capture shim derives decisions from wrapper exit status alone and does not parse event-specific JSON denials. The existing `TestApplyLiveHookProbeRecordsSeparatesObservedFromComplete` expects Enforced after one generic blocked record; update that expectation with the causal-proof implementation because it currently encodes the incorrect inference. Actual CLI help was inspected without launching agent runs: Codex offers exec JSONL/ephemeral/ignore-user-config and explicit hook-trust control; Devin offers print, explicit config, permission mode, and workspace trust; Cursor offers print/stream-json/workspace/trust; OMP offers print/json/cwd/no-session/config. Qualify exact combinations and keep approved credentials separate from disposable session/config state.

Implementation checkpoint: typed optional `host` and `probe` fields extend the existing CLI report without changing its format identity; no registered hook-verification JSON schema was found. Host discovery now prefers `cursor-agent`, positively checks its Cursor banner, checks branded Codex/Devin/OMP version output, resolves and fingerprints the entrypoint before/after bounded metadata subprocesses, and keeps other legacy identities explicitly unqualified. Codex and Cursor use script launchers on this host: the entrypoint digest is deliberately not presented as the digest of their entire native runtime. DSH discovery remains part of the pending platform integration in TASK 550.

Prepared receipts bind a fresh run ID, timestamp, Reconc executable digest, repository path identity, configuration-file digests, and seven distinct operation nonces. Configuration drift, duplicate file identities, escaping paths, and changed preserved wrappers are rejected. All operation outcomes initially remain unproven. Route-only observations no longer imply enforcement, and contradictory route-class/exit-status pairs are rejected. Text-mode operator confirmation now runs in a bounded child under a five-minute overall deadline; JSON mode never reads operator input. POSIX cancellation terminates the owned process group; Windows preserves bounded direct-child cancellation without claiming process-tree termination.

A real policy regression initially proved that adding the command marker to deny_write still allowed `touch forbidden-command-marker`. The disposable policy now also contains an explicit forbid_command rule for that exact command. The unchanged regression passes for both denied operations and both allowed controls. This is synthetic policy evidence, not host execution.

Checkpoint verification: the initial `make test-fast` passed both Go modules before receipt construction was added. Focused receipt/identity/timeout/policy tests and their race run subsequently passed (race: 5.688 seconds); logs are `.build/task545/test-fast-initial.log` and `focused-race-receipts.log`. The complete fast gate for the final receipt/policy state also passed both Go modules and reference/format checks (`test-fast-receipts.log`); `go vet ./internal/cli` passed. TASK 544 dependency commit `fa34c3c8a08d3a01112ed54f5a9385dce6b38e8a` is verified on origin/main with all applicable hosted checks successful: CI run `34714510822` and CodeQL run `34714510850`; Windows smoke was intentionally skipped. Exact-commit check evidence is retained in `.build/task544/hosted-checks.json`.

Native Codex investigation: the installed `codex-cli 0.154.0` supports bounded ephemeral `exec --json` runs. A harmless read-only stream probe passed. Two isolated three-operation probes initially produced no hook events because the project configuration was not trusted. Offline `app-server` `config/read` and `hooks/list` proved that CLI override `projects."<path>".trust_level="trusted"` retained literal quotation marks in the project key and disabled the actual project's configuration layer. Passing the complete TOML value `projects={"<canonical-path>"={trust_level="trusted"}}` instead loaded all 13 generated hooks. The independently required hook-definition review was bypassed only for this vetted temporary invocation using Codex's documented flag; no saved global trust or host/skill configuration was changed. Evidence: `.build/task545/codex-config-inspection.json` and `codex-config-fixed-inspection.json`.

With the corrected ephemeral project override, a real Codex run completed in 13.425 seconds: the allowed shell marker was created, two blocking pre-hook records were observed, and neither forbidden marker existed. However, the `exec --json` stream omitted separate item events for the rejected operations, and the then-current route-only capture did not retain their call/input identities. The assistant's final text is not authoritative native rejection evidence, so the product verifier correctly remained incomplete. Exact streams, capture, flags, and effects are retained under `.build/task545/codex-hook-map-trust-inspection/`. The two earlier negative controls remain under `codex-hook-inspection/` and `codex-hook-trusted-inspection/`. Current primary contract references: https://learn.chatgpt.com/docs/hooks and https://learn.chatgpt.com/docs/non-interactive-mode; local installed app-server schemas are under `.build/task545/codex-app-server-schema/`. TASK 546 must select a native event interface that retains rejected call IDs/results before claiming causal enforcement.

Capture checkpoint: the shell-only jq logger is replaced by a bounded Go capture subprocess that preserves actual wrapper stdout/stderr/exit status and binds run/time, exact delivered payload and response digests, and available native session/call/tool-input digests. It handles event-specific exit-zero JSON denial encodings, rejects ambiguous JSON and contradictory records, checks probe age/run identity and duplicate session/route/call IDs, bounds input/output, and confines record-file writes to the disposable repository. Only exact native source keys are bound; unsupported host identity aliases remain absent until qualified by their adapter. Total wrapper wall time replaces the prior internal-runtime-only timing in live capture. Focused contract/race tests passed before transport tests (race 1.427 seconds); real CLI/evaluator subprocess transport and symlink controls subsequently passed in 0.507 seconds. Logs: `.build/task545/capture-race-tests.log` and `capture-transport-tests.log`.

The complete fast gate passes for the final capture reader and tool-name binding state (`.build/task545/test-fast-capture-final.log`, both Go modules). `go vet ./internal/cli` and `git diff --check` pass. Focused reader/transport race tests passed in 4.377 seconds before the final tool-name field addition (`capture-final-race.log`). A separately built CLI exercised the complete generated shim and preserved wrapper in isolated Codex and Cursor installations: both denied controls were recognized; Codex returned `[0,2,0,2]`, Cursor returned `[0,0,0,0]` with JSON decisions `[allow,deny,allow,deny]`. Both reports remained incomplete with `enforced=false`. Evidence: `.build/task545/capture-generated-smoke/`. These are synthetic generated-transport controls, not additional native agent runs. The first Cursor smoke invocation used the wrong surface spelling and was rejected before setup; the successful invocation used the registry's exact `cursor-cli-print`.

Policy-provenance checkpoint: `Result.PolicyDecision()` now exposes the existing internal classification only for complete error-free evaluations or validated cache hits, before host response adaptation discards that classification. The existing timing descriptor additionally carries `policy_decision=pass|block|unproven`; capture reads it from a private bounded pipe with a deadline, preserving the actual host response. A malformed payload and a corrupted real lockfile still produce transport denials for Codex/Cursor but now retain `policy_decision=unproven`. Successful policy passes and blocks are distinguished from unclassified reads. Encoding failures invalidate classification. Windows retains explicit unproven provenance because Go does not support inherited ExtraFiles there. Capture also binds exact native `turn_id` and command digests. Focused real-runtime transport and malformed-policy race controls passed in 9.969 seconds (`policy-provenance-race.log`); the expanded two-package race run passed CLI in 10.468 seconds and agentsession in 1.224 seconds (`policy-and-native-parser-race.log`).

Native outcome discovery: inspection of the complete already-captured Codex stderr found authoritative router diagnostics for both rejected commands, including the multiline apply_patch input. These are emitted by `codex_core::tools::router`, separately from the assistant's JSON messages. `parseCodexNativeRejections` now accepts only that exact timestamped framing, retains timestamps and command digests, and rejects truncated, ambiguous, missing-command, or oversized records. The byte-preserved public rejection entries are in `internal/cli/testdata/codex-native-hook-rejections.txt`, SHA-256 `ae5727dbe4b9eb3dfaf080164bca3cdb313f40e60535a45d489d69d53f42ad3f`; the full native source remains `.build/task545/codex-hook-map-trust-inspection/host-stderr.log`. Parsing and adversarial framing controls pass. The native runner must own the stderr stream and match a unique nonce-bearing command to its captured call and independent side effect; accepting arbitrary diagnostic files as native proof is forbidden. This diagnostic protocol is version-qualified evidence, not a stable public JSON API. The parser is not yet wired to a native runner, so no enforcement claim has been added.

An alternative app-server startup probe used an empty temporary HOME/CODEX_HOME, initialized one ephemeral thread, and unsubscribed without sending a model turn. It produced no hook observation and its unauthenticated speculative websocket connection received 401. The server exited cleanly. Evidence: `.build/task545/codex-app-hook-start/`. No auth file was copied or user configuration changed. `HookCompletedNotification` contains status and thread/turn but lacks call IDs, and `experimentalRawEvents` is marked internal-use-only. Prefer the demonstrated `exec --json` plus native stderr rejection path for the runner; do not continue app-server/auth experiments unless that path proves insufficient. The installed npm launcher's source was read: it resolves the platform optional package's vendor binary, otherwise its own vendor directory, and forwards signals. Bind the native executable as well as the launcher before qualifying execution.

Remaining closure: finish independent effect/outcome negative controls, review the integrated native runner and its failure diagnostics, and pass the complete task gates. The Codex runner now proves all four native write/shell controls, while read, nonzero-shell, classified-MCP, other host runners, and additional lifecycle qualification remain explicitly assigned to TASKS 546-550 and 555. DSH discovery/registration remains dependent on TASK 550. Do not archive or commit TASK 545 before its shared contract, regression corpus, documentation, and gates pass. Authenticated runs use isolated temporary repositories and preserve the configured authentication provider; no existing user conversation is resumed.

Native-correlation checkpoint: the fast gate for policy provenance completed successfully for both Go modules (`.build/task545/test-fast-policy-provenance.log`). Subsequent review found Go's legacy JSON struct decoder accepted case-insensitive aliases despite the exact-key capture contract. Command and decision decoding now uses the standard library's case-sensitive JSON decoder; persisted records also reject unknown members. Alias and conflicting-key regressions pass. The native Codex JSON stream parser binds exactly one session and completed turn, pairs command starts/results by exact item ID and command, and rejects replay, missing results, malformed JSON, unknown event types, contradictory status, truncation, and bounded-output violations. Its positive fixture, `internal/cli/testdata/codex-native-command-stream.txt`, is byte-identical to the previously captured real `host.jsonl`; assistant messages are ignored as evidence. A separate denial correlator requires one captured policy block and one timestamped native rejection for the exact command, matching session/call/tool identities and the single captured prompt turn. Wrong runs, turns, sessions, duplicate commands/rejections, and early/late outcomes remain unproven. Focused race controls passed (`native-correlation-race.log`, 10.673 seconds, before the final prompt-turn checks; final checks in `native-correlation-turn-race.log`). These parsers and correlation checks are not yet wired into an owned native host runner; they do not establish executable closure, nonce-specific positive controls, independent effects, or whole-probe enforcement. No new authenticated run, commit, push, or task closure occurred at this checkpoint.

Integrated native-runner evidence: `--live --host codex --surface cli --allow-authenticated --json` now runs the qualified native binary directly after resolving its npm installation layout, validating its version, and binding both launcher/runtime digests. Invocation-local trust activates the prepared project configuration for this exercise; saved configuration is untouched. The successful run in `.build/task545/owned-native-join.json` lasted 15.055 seconds with native exit 0, `loaded=true`, `observed=true`, and `enforced=true` for four independently complete write/shell controls. Both positive marker contents matched their fresh nonces; each denied command matched its own capture, policy block, native rejection, and absent effect. Whole-report `complete=false`/degraded is intentional until the remaining operations and lifecycle matrix are qualified. Native runtime SHA-256: `4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`; launcher SHA-256: `61b0194f3bb6534439c8d26a3ed57d0805f84b884588b761795323eeb92fcf70`.

Two native differences were diagnosed rather than accepted as passes. `.build/task545/owned-native-diagnostics.json` proves the installed Codex emits `item.started` for file changes; the parser now checks matching start/completion identities while retaining the documented completion-only shape. `.build/task545/owned-native-qualified.json` rejected unproven capture process-group ownership. The exact [Codex 0.154.0 command runner](https://github.com/openai/codex/blob/rust-v0.154.0/codex-rs/hooks/src/engine/command_runner.rs) creates a distinct hook process group and replays the session environment. Capture now joins the live native owner before spawning policy children and records that group. The resulting real native run proves that binding works. Focused race controls passed (`native-owner-final-race.log`, 11.212 seconds); separate real-process tests prove cancellation across a separately grouped capture child and cleanup of descendants after their native owner exits (`native-cleanup-race.log`, 1.846 seconds). SIGINT/SIGTERM now cancel the live context; native cleanup has its own two-second limit. The first broad fast gate failed only because the stricter JSON decoder changed the expected unknown-field wording. The production error was normalized back to the stable `unknown field` contract without changing the test. A subsequent full gate is still required for closure.

Final shared-contract review: all five requested hosts now have bounded executable/version discovery definitions. DSH requires the unbranded package version and the positive `dsh: boot a DeepSeek Harness profile` help identity. This contract comes from pinned upstream [args.ts](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/apps/cli/src/args.ts) and [bin.ts](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/apps/cli/src/bin.ts); both metadata paths exit before profile boot. Positive and wrong-brand/version subprocess controls cover identification. Public DSH platform registration and native execution remain TASK 550, as specified in the dependency plan. Text reports now retain the full compact host/probe receipt without terminal-line injection. Native stream controls reject an item changing between file and command types. A second real native run (`owned-native-reviewed.json`, 16.326 seconds) independently completed the same four controls. Staticcheck found capitalized parser error strings; their wording was corrected without relaxing assertions or disabling checks.

Closure verification: `make test` passed the complete uncached race suites for both Go modules and release trust (`full-test-native-runner.log`). The final source additionally passed `make test-fast` for both modules (`test-fast-closure.log`), the final discovery/parser race controls (`discovery-parser-final-race.log`), `make lint`, `make vet`, and `make self-host` (`lint-closure.log`, `vet-closure.log`, `self-host-final.log`). The publication audit of the staged implementation passed for 1,972 tracked files (`publication-staged.log`). All 25 protected private historical task files retain their original hashes. No Windows execution was requested or performed.

Acceptance review: executable/version/digest identity and seven independent operation slots are present in JSON and text receipts; native Codex owns and bounds the actual subprocess, capture, configuration, and marker checks; foreign, replayed, incomplete, malformed, and failed evidence stays unproven. All five discovery contracts are defined, with the DSH adapter still owned by TASK 550. The shared verification foundation is complete. The four native Codex write/shell controls are the demonstrated host subset, not a claim that TASKS 546-555 or the complete live matrix are finished.

## Deviations

None. No authenticated host probes were launched during planning.
