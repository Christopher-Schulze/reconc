# TASK 550: Add native DeepSeek Harness integration

## Why

DSH is explicitly requested and absent from the platform registry. Its Claude/Codex bridges provide partial compatibility, but the inspected Claude bridge loses typed outcomes and continues after some configuration/hook failures. Native Cordis/tool-runtime integration offers a stronger path.

## Acceptance

- DSH is a first-class platform with generated installation artifacts, typed normalization, capability/status reporting, removal ownership, schemas, fixtures, and documentation.
- A native adapter gates supported tool execution through the existing Go engine and records truthful typed outcomes with repository/session/call binding.
- Missing decisions, worker errors, and late/short-circuited listeners cannot silently allow protected operations while the native enforcement extension is active.
- Native, nested/programmatic, subagent, cancellation, and multi-repository behavior have explicit tests and capability boundaries.
- The extension is loaded through a supported DSH profile mechanism without overwriting user profiles or adding a runtime dependency to the Reconc binary.
- A version-bound live result exists, or qualification remains explicitly blocked with the exact host limitation. A compatibility-only demo is not task completion.

## Sub-Tasks

- [x] Prove the native extension and immutable execution guard design against pinned upstream.
- [x] Add the DSH registry/generator/installer/normalizer and reuse the bounded worker transport.
- [x] Integrate truthful outcomes, lifecycle, context injection, and profile ownership.
- [x] Add generated-extension execution tests and isolated DSH integration tests.
- [x] Propagate all platform references and qualify the installed DSH CLI.

## Technical Plan

1. Use the published `dsh-v0.1.5-rc.2` release tag `fb2c4b9e698e30edb738bca4cf0618587db7d203` as the supported source pin. Its relevant runtime, agent, profile, and base-bundle blobs match the initial master baseline `c291e7961a515f6d7af9304e7fd1d257929aef26`.
2. Extend `internal/hooks/platforms.go`, generator/install/status/removal owners and the platform-name/host-event tests. Add narrowly scoped new `internal/hooks/dsh.go`, `internal/runtime/agentsession/dsh.go`, and their tests, following the existing OMP organization. Propagate enum/schema/CLI/bootstrap/reference consumers through a repository-wide reference search; do not hand-maintain a second platform list.
3. Generate a thin Cordis extension. `tools/pre-execute` receives an asynchronous decision continuation; `ToolRuntime.guard` accepts a synchronous function returning a denial reason or undefined. JSON arguments are frozen, but the execution object's identity properties are writable until result notification. The final guard must verify its decision-bound identity and make the name, arguments, agent, token, and call identifiers non-writable before `tools/execute` runs. Do not invent an asynchronous guard.
4. Proposed enforcement design: evaluate asynchronously through the existing Go worker in pre-execute; store the completed decision against the opaque execution token and immutable argument/repository identity. Register a synchronous monotonic final guard that denies governed operations when that decision is absent, stale, denied, or belongs to another execution. A listener that skips the rest of the pre waterfall must not bypass this final guard. Use bounded lifecycle-owned state or weak token ownership; clean up on result, cancellation, session disposal, and extension disposal. Prove this ordering with the actual ToolRuntime before integrating the product adapter.
5. Normalize callId/rootCallId, agent/session identity, CWD, AbortSignal lifecycle, direct file/shell operations, and MCP envelopes. Inspect the native tools' exact input/output schemas before alias mapping. Exercise nested programmatic calls and modes separately; raw arbitrary language execution is not automatically a parsed shell policy boundary.
6. Native tools/result observes a frozen final result after post-execute transformation. Distinguish that final host result from original process evidence. Post middleware can replace a successful value, so a success-shaped shell result alone cannot certify its original exit status. Use trusted tool-owned execution evidence where available; otherwise record observation and require the existing reconc exec/CI backstop for command proof. Never restore the bridge's text-only tool_response approach.
7. Establish policy/context before the first protected operation; detached session-start notification is insufficient. Use supported agent pre-step/turn lifecycle hooks for awaited setup, compaction reinjection, and bounded completion steering. Verify per-session versus per-turn scopes and subagent ownership; do not introduce unbounded stop loops.
8. Inspect apps/cli/src/profile-boot.ts and the actual profile format before generating an owned import/config fragment. Preserve unrelated Cordis plugins, existing profile code, and other repositories. Status must detect missing/disabled profile imports and duplicate bridge+native routes. User profile installation is an explicit integration action, not a side effect of skill installation.
9. Reuse the existing Bun test runtime and worker transport. Pin required upstream test packages only after package/license/size review; shipped Reconc remains a Go binary. If the pinned upstream offers no sufficient immutable guard/profile-loading contract, stop this implementation at that concrete blocker rather than shipping false native-enforcement claims.

## Verification

Run `go test ./internal/hooks ./internal/runtime/agentsession ./internal/cli ./internal/schema` plus real generated-extension execution using the pinned DSH ToolRuntime. Required controls: allowed/denied edits and shell, short-circuiting pre listener, worker crash/timeout, changed arguments, failed/canceled/transformed result, nested tool call, concurrent sessions/repositories, first-turn race, duplicate bridges, disabled extension, profile reload, stop bound, and disposal. Provider-backed DSH tests belong to TASK 555; offline runtime execution must not be mislabeled a live model test.

## Dependencies

TASK 545. Reuse verified transport/lifecycle improvements from TASK 549. TASK 551 owns shared skill discovery; TASK 553 owns compact guidance.

## Sources and Evidence

- [Native tool runtime and guard](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/packages/core/tools/src/index.ts).
- [Agent lifecycle](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/packages/core/agent/src/index.ts).
- [Claude compatibility bridge](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/packages/hooks/hooks-claude-code/src/index.ts) and [configuration parsing](https://github.com/deepseek-ai/deepseek-harness/blob/c291e7961a515f6d7af9304e7fd1d257929aef26/packages/hooks/hooks-claude-code/src/config.ts).
- Sources inspected on 2026-09-12 and retained under `.build/planning-agent-integrations/dsh/`. No DSH executable was found on PATH.

## Notes

The native adapter is in progress. Its generated extension, registry, installer, normalizer, and disposable runtime proof are uncommitted. The synchronous guard constraint and transformed-result boundary remain mandatory qualification checks. The current worktree is not ready for a TASK commit or push.

Entry: TASK 549 was pushed as `7f6786d92c241a9df63da82f682e0825bbb5676b`; the worktree was clean. Upstream `master` still resolves to the planned `c291e7961a515f6d7af9304e7fd1d257929aef26`, while npm publishes `@deepseek-ai/dsh@0.1.5-rc.2` under `next` (the default `latest` tag remains rc.1). No `dsh` executable is on PATH. The DSH CLI and ToolRuntime packages are MIT-licensed, and no Node dependency has been added to Reconc.

Pinned ToolRuntime source confirms that `tools/pre-execute` is asynchronous, `tools.guard()` is a synchronous monotonic gate after the complete pre waterfall, arguments are JSON-materialized and frozen, and `tools/result` receives the frozen final outcome. A disposable Node 26 test using published `@deepseek-ai/dsh-tools@0.1.5-rc.2`, Cordis 4.0.2, and the system-prompt service registered an async pre decision and token-bound guard: an ordinary call reached the body once; a prepended pre listener returned `allow` without calling the Reconc listener, and the final guard denied it with no second body execution. This proves the planned short-circuit defense on the actual published runtime, not merely on a copied signature.

The published `dsh-v0.1.5-rc.2` tag resolves to `fb2c4b9e698e30edb738bca4cf0618587db7d203`. It precedes `master` by 139 commits, but the Git blob IDs of the inspected ToolRuntime, agent registry, CLI profile boot/arguments, and base bundle files are identical at both revisions. The npm CLI was installed only in a disposable `/tmp` prefix (520 packages, 281 MiB), not globally or in Reconc; its real `--version` reports `0.1.5-rc.2` and its help exposes the documented `--patch` overlay. Use the release tag as the supported source pin, with npm integrity `sha512-8Xc8hCQHcIWRmTCVU/xZdp6/qMsWMeAd2ObChKDEsfhUPJFXx6H0lgeb1DxUMD86HZrrVN+1bCvn1ppjZ/fOxw==`.

Profile source and documentation show that `--patch` is a supported invocation overlay after profile/user layers; profile rows activate by service availability, not list order. The repo-owned adapter must therefore prove it mounts before first protected dispatch, and a patch file's position alone is not an enforcement guarantee. Profile ownership and injection remain under review before installation code is chosen.

A disposable overlay inserted a plugin that injects `tools` and provides `reconcGuard`, and amended the base `agent-loop` row to inject `reconcGuard`. The installed CLI's `--dump-config` retained the original agent-loop configuration and showed that dependency plus the plugin's file URL; a real `dsh --profile headless --patch ... --help` boot reported the plugin mounted and exited successfully. This proves profile composition and loading, not a model-driven tool call or policy denial. The service dependency is the candidate first-agent gate; all profile modes and direct programmatic calls still need explicit scope checks.

The execution object itself is mutable until `tools/result`; the runtime invokes `tools/execute` after the synchronous guard. Its source contract permits only signal replacement there, but JavaScript does not enforce that type restriction. A second real-published-runtime probe made the guard mark call identity and arguments non-writable and registered a later `tools/execute` listener that tried to replace arguments. The mutation threw `TypeError`, and only the authorized body ran. The session header is documented and implemented as detached, deep-frozen metadata, including its `cwd`; the guard still must bind its reference and repository identity to each decision. This changes the implementation requirement from merely checking a frozen argument object to protecting the complete execution identity before dispatch.

An isolated generated-extension test using the published ToolRuntime returned denied=true for a protected write, allowed=true for an ordinary write, skipped=true for a prepended short-circuiting pre listener, and executed the body exactly once. The first run found that the extension's returned disposer was not registered with Cordis and left the Go worker alive; registering it through `ctx.effect` fixed the lifecycle, and the same test then exited cleanly. This is an offline host-runtime proof, not a provider-backed DSH tool call. The registry, generator, normalizer, portable audit, and documentation tests are now present; full task qualification remains open.

The generated-adapter offline test executes the real Go worker for policy allow/deny and uses a deliberately missing wrapper and a nonresponding wrapper to prove that worker startup failures and handshake timeouts deny through the final guard. It also checks cancellation, changed repository identity, cross-repository workdirs, unsupported PowerShell, and a compatibility bridge loaded between pre decision and guard. A separate Go scenario proves that DSH's transformed final result creates no material write or shell-success evidence.

The published Cordis runtime proved that a plugin injected with `reconcGuard` stays pending before the native extension mounts, unloads when its guard service disposes, and restarts only after the extension reloads. The pinned headless `--dump-config` output contains the generated `agent-loop` dependency and the enabled `reconc-native` row with `tools` and `systemPrompt` injections. This proves profile composition and service lifecycle in the published runtime, not a provider-backed model run.

The extended published-runtime probe on the disposable `@deepseek-ai/dsh@0.1.5-rc.2` installation now also confirms prompt-context assembly, late argument mutation rejection, nested programmatic dispatch denial, unbound and cross-repository denial, two concurrent same-repository sessions, a child-shaped session, cancellation before dispatch, rejection of a registered `hooks-codex` compatibility bridge, and guard restoration after Cordis fiber disposal/reload. Output: `{"denied":true,"allowed":true,"skipped":true,"nested":true,"unbound":true,"foreign":true,"concurrent":true,"child":true,"canceled":true,"duplicate":true,"reloaded":true,"bodies":4}`. This is real ToolRuntime execution with an isolated Go policy worker, not a provider-backed model call or proof of the complete DSH subagent lifecycle. The built-in DSH tool names are lowercase `read`, `write`, `edit`, `bash`, and `pwsh`; the native `write` path is used in repository verification. `pwsh` is currently denied because Reconc's Bash parser cannot safely claim PowerShell policy coverage. Bash workdirs outside the repository are denied. The extension's missing-decision guard now denies cross-repository calls even when a preceding listener skips Reconc.

Published DSH file and shell tools resolve relative arguments against the session CWD or explicit Bash workdir. Reconc's policy normalizer resolves relative paths against the repository root. The adapter now requires both identities to equal the canonical repository root, rather than merely remain inside it; subdirectory sessions and Bash workdirs fail closed in both the generated extension and Go normalizer. The generated-extension verifier and normalizer tests cover this mismatch.

A platform-reference pass exposed two missed owners. The command metadata and custom-runtime reserved-name list did not yet include DSH; registry-parity tests exposed both. More seriously, the generic bootstrap accepted `--hook dsh` but rendered only `.dsh/reconc.mjs`, so its own verification rejected the missing patch. Transactional rendering now includes the generator-owned `.dsh/reconc.patch.yml` and treats the exact static `installed` state as ready for an explicitly activated host. A governed `BuildPlan`/`Apply`/`Verify` test checks both artifacts, wrapper, and honest status. The portable bootstrap runbook, scaffold docs, command reference, skill, and embedded agent guide now describe DSH's activation boundary.

The first bootstrap integration test exposed a second owner gap: the portable repository receipt classified the DSH patch as user-owned, so `repo sync` could not update it. `syncOwnsComponent` now recognizes only the exact DSH activation component as an owned file, leaving Codex's managed-block activation unchanged. The same governed test now requires receipt verification and an unchanged sync action for both DSH artifacts.

The latest generated extension was reinstalled into the disposable scenario using a freshly built unversioned development binary. The pinned DSH `--dump-config` again shows `reconcGuard` injected into `agent-loop` and the file-backed `reconc-native` plugin; headless boot exits zero. The published ToolRuntime probe again returned `{"denied":true,"allowed":true,"skipped":true,"nested":true,"unbound":true,"foreign":true,"concurrent":true,"child":true,"canceled":true,"duplicate":true,"reloaded":true,"bodies":4}`. This proves the current generated bytes against the published runtime, still without a provider-backed model call.

The generated-extension verifier now sends native `edit` and `bash` arguments to the real isolated Go worker as well as `write`: protected edits and the `touch forbidden-command-marker` command are denied, ordinary edits and `pwd` are allowed, and final guarded decisions are asserted. The focused offline verification test passes; no shell body is executed by this contract probe.

Diff review corrected the registry's adapted session-start origin from `tools/pre-execute` to the actual primary `agent/pre-step` listener. The generated worker route remains a synthetic `session_start` initiated before the first step, with a pre-tool fallback for nested/programmatic dispatch; its host-event fixture now records the published pre-step route.

The same review found that the serial Go-worker queue, passive-result observation set, and started-session map could grow without a shared cap under heavy programmatic dispatch. The extension now bounds each at 512 entries, denies new protected calls when session/queue capacity is exhausted, and drops excess passive observations with one diagnostic rather than allowing unbounded memory growth. A generated-extension probe drives 513 distinct pre-step sessions through the actual worker protocol and expects 512 entries plus one rejection.

The checked-in generated-extension verifier also binds a nested child-shaped call and two simultaneous tool decisions, rejects a post-decision argument swap, and exercises cancel/reload and duplicate-bridge boundaries. These durable tests use the actual Go worker with a minimal Cordis event driver. The separate pinned ToolRuntime probe proves the host's real middleware ordering but is disposable evidence; a provider-backed end-to-end run and complete subagent lifecycle remain TASK 555 qualification work.

Fresh-eyes verification found that `hook verify` replaced its disposable transport stub with a real DSH worker only when running inside a Go test binary. A built product CLI therefore had no worker at the expected path. Production verification now installs a verified copy of its running CLI executable through the existing bounded copy helper; Go tests retain their source-build path. A regression test builds the product CLI and runs `hook verify --host dsh --json` through that production path, requiring complete transport, policy-decision, and response-adaptation evidence.

Final gates: `make test` (root and portable race suites, publication audit, deterministic harness pack, and release trust), `make vet`, `make lint`, `make build`, `make self-host`, the built-CLI DSH verifier, and its targeted race test passed. The profile load and native ToolRuntime proof are pinned to the published DSH package above. Provider-backed model execution and complete subagent lifecycle remain unqualified here and are assigned to TASK 555; no release was published.

## Deviations

No host or plugin was installed globally. Disposable DSH packages and an isolated repository were used to qualify the extension.
The pinned host uses a supported explicit `--patch` overlay, not a repository auto-import. Static status therefore reports `installed` with `configured=false` until a host route is observed; it cannot infer an unrelated DSH process's launch arguments. The patch makes `agent-loop` depend on the extension's `reconcGuard` service, so disabling the native plugin within that patch prevents normal agent-loop startup. A programmatic caller that deliberately omits the whole patch is outside the native guard; the installed files alone never claim otherwise.

The advanced harness pack is a tracked deterministic manifest plus ZIP. Updating the portable DSH scaffold required the project's canonical pack generator to atomically replace those two generated artifacts. Before publication, a disposable candidate was built and its exact manifest diff, new `.dsh` entries, ZIP integrity, file count, and digest were reviewed; the in-repository output was then verified byte-identical to that candidate. No product release number was assigned or changed.
