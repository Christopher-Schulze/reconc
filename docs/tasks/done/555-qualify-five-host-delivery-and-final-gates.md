# TASK 555: Qualify five-host delivery and final gates

## Why

Adapter unit tests, installed files, binary checks, native host enforcement, hosted CI, and published release assets prove different things. The requested reality check needs one final evidence matrix for the integrated product and explicit remaining limitations.

## Acceptance

- Codex, Devin CLI, Cursor CLI interactive/print, OMP CLI, and DSH each have a version/config-bound evidence record that separates source/API compatibility, offline contract results, prior host observations, and unproven operations.
- Normal install, local skill ownership/discovery, missing-skill suggestion, stale-owned-skill update, same-binary repair, and preserved user modifications are exercised through real CLI flows; actual host-side skill loading remains unproven without an agent-host run.
- Every supported enforcement claim has a positive control and a linked denied-action proof. Unsupported/blocked/unproven behavior is individually named and cannot be aggregated into full green.
- Required macOS/Linux gates and reference/artifact checks pass for the final source; prior authenticated live results and exact-commit hosted CI are reported separately. No new agent-host run is required or performed.
- Documentation is consistent with measured capabilities, installation/update behavior, benchmark results, and residual limits.
- All implemented tasks are archived only after their acceptance and validation; commits and any authorized pushes remain one task at a time on main.
- No product tag, release publication, or assigned future version is created by this task.

## Sub-Tasks

- [x] Assemble the exact-version probe matrix and verify prerequisites without changing active user sessions.
- [x] Review pinned host source and run isolated offline adapter and install/update scenarios, recording outcomes and boundaries without another agent-host run.
- [x] Fix the in-scope built-CLI, terminal, Cursor capture, and OMP worker-shim discrepancies; record unqualified live-verifier correlation as a limit.
- [x] Run final local verification and update the canonical documentation; bind hosted results to the pushed commit separately.
- [x] Re-read changed files and task evidence; archive and report only verified completion.

## Technical Plan

1. Use TASK 545's single verifier/receipt contract. Record host executable/version/digest where previously observed, Reconc source/build, adapter/config identity, mode, skill identity/discovery, policy/candidate identity, attempted operation, decision, result, side effect, and timestamp. Mark absent native outcomes unproven.
2. Review the pinned host APIs and generated artifacts for each applicable mode: allowed read/write/shell, denied write/shell, nonzero shell, classified MCP, first-turn context, compaction, cancellation, session completion, and subagents/background operations where supported. Cursor interactive and print have separate records. Retain prior host observations without extending them to unobserved routes.
3. Run offline controls for missing, disabled, or shadowed hooks where the generated adapter can be exercised without launching an agent host. A static configuration or synthetic event proves only its stated contract, not native host enforcement.
4. Do not launch DSH or another agent host for this task. Inspect the published `dsh-v0.1.5-rc.2` source, CLI/profile documentation, generated extension, and offline tests. Provider-backed DSH behavior stays explicitly unqualified; a global DSH installation or model credential is not an implementation prerequisite.
5. Exercise official install flows in temporary homes, inspect local skill location and ownership, and leave host-side discovery/loading unproven. Run update check/apply across the TASK 552 state table. Verify archive/binary embedded payload equivalence and release-trust fixtures without publishing a release.
6. Final local gates: `make test-fast`, `make test`, `make vet`, `make lint`, `make self-host`, `make coverage` for both modules as review evidence, and `git diff --check`. `make test` already includes publication audit and release trust; rerun a phase only for changed inputs or an unresolved failure. TASK 554 owns the complete benchmark evidence. Maintain free-disk/cache checks before heavy work.
7. Preserve Windows implementation and release artifact definitions. Do not run automatic Windows suites; only an explicitly requested smoke check with a hard two-minute job limit is allowed. Complete Linux/macOS validation remains the product gate.
8. Flush product behavior into `docs/documentation.md`; update README, generated CLI/reference docs, embedded guide, and portable skill references only where their promises changed. Keep task history/evidence in task details. Preserve documentation for other hosts and the existing optional MCP gateway.
9. During implementation, finish and verify one task before its commit, inspect its diff, archive its detail with a true move, and update docs/tasks.md. Push only within the user's actual execution authorization, to origin/main. After a push, bind GitHub CI/CodeQL/artifact checks to the exact remote commit and report pending/failure honestly. Never infer hosted green from local checks or push success.

## Verification Matrix

| Dimension | Required proof |
| --- | --- |
| Adapter contract | Generated configuration/extension executes against the pinned host API |
| Native enforcement claim | Requires a previously observed host attempt, matching deny, and independent unchanged target; otherwise unproven |
| Positive control claim | Requires a previously observed allowed operation on the same host/config; otherwise unproven |
| Tool evidence claim | Requires a native typed outcome or trusted command receipt, not hook exit or prose |
| Skill delivery | Verified bundle, owned install receipt, local discovery path; host loading separately unproven |
| Update | Read-only check; correct missing/stale/modified state; transactional apply/recovery |
| Performance | TASK 554 paired source-bound artifacts and complete target inventory |
| Local/hosted checks | Exact commands and commits, individual outcome, no stale aggregate green |

## Dependencies

TASKS 544-554. This is the final integration gate, not a second owner for each host implementation.

## Shared Execution Contract

All tasks in this batch are queued plans created on 2026-09-12. Before starting implementation, re-read the task board, detail, actual source signatures, current host versions, and applicable AGENTS.md. Revalidate drifting upstream docs before coding. Apply minimal patches, preserve unrelated/dirty files, use isolated repositories, run focused tests per logical sub-task and the required completion gates, and keep claims tied to retained evidence. This task can close with explicitly unqualified host operations under the user's revised no-agent-run scope; closing it does not turn those operations green.

## Notes

Starting source: `25ce22668769dbbb8c2360f8db6e7ecc574033e1`. Planning inspected registry/generators, native runtime adapters, live verification, install/update ownership, skill content/release assets, release-trust classification, retained benchmarks, current official docs, and pinned DSH/OMP source. Source URLs, file sizes, and SHA-256 digests for upstream files were recorded under ignored `.build/planning-agent-integrations/`; the release-trust gate removes that disposable directory, so public pinned source links and the evidence below remain the durable references. The planning pass did not execute authenticated live host enforcement, a new full benchmark, or current hosted CI for modified product code. No product implementation was changed in that pass.

The native-hook boundary cannot prevent an operator from disabling/unloading the host extension or bypassing the host entirely. Documentation must identify those limits and the deterministic CLI/Git/CI backstop without presenting a skill or MCP gateway as universal enforcement.

Qualification start at source `371764ff0ffb0acfc14657e342b76cb04093d014` on 2026-09-13: local Codex CLI `0.154.0`, Devin CLI `3000.10.21 (611c1cba)`, Cursor Agent CLI `2026.09.10-fd3934a` at `~/.local/bin/cursor-agent`, and OMP CLI `18.1.18` are present. The editor command `cursor` is not on PATH, which does not imply the Agent CLI is absent. `dsh` is absent on PATH; the official upstream npm package is `@deepseek-ai/dsh`, with registry version `0.1.5-rc.1` during this inventory. Node `v26.8.2` and npm `11.19.1` are present. Installed global Reconc reports `dev+858f152b6df9-dirty`, so it is not the just-pushed source; live probes must use the task-built binary, never assume the global command is current. GitHub CI and CodeQL runs for `371764ff` were in progress at the first exact-commit check. These are prerequisites and inventory, not live enforcement results.

The supported DSH release is `0.1.5-rc.2` from the `next` dist-tag, with the exact package integrity pinned in TASK 550; npm's default `latest` still resolves to `0.1.5-rc.1`. An isolated `/tmp` prefix installed rc.2 (520 packages, about 280 MiB); `dsh --version` and the DeepSeek-branded help passed. A fresh `hook verify --live --host dsh --surface cli --allow-authenticated --json` verified its executable identity and generated configuration but returned `complete=false`, `loaded=false`, and seven unproven operations because JSON mode correctly does not launch this operator-assisted host. An isolated DSH profile's `--dump-config` contained the generated `reconcGuard` injection into `agent-loop` and the file-backed `reconc-native` plugin; headless `--help` boot exited zero. No DSH model credential or model selection was present, so no provider-backed tool call is claimed.

A clean clone of commit `371764ff` produced `reconc dev+371764ff0ffb`. Its native Codex CLI live probe completed in 18.58 seconds with `loaded=true`, `observed=true`, and `enforced=true` for the four independent allowed/denied write/shell controls; both denied actions had native `rejected` outcomes and absent effects. Read, nonzero-shell, and classified MCP remained unproven, so the overall report exited 2 with `complete=false`. The executable and runtime digests matched the already qualified installed Codex 0.154.0. A separate temporary-home `install-cli --install-dir ... --skill-dir .../reconc --json` installed this exact binary and its 4057-byte embedded skill with receipt checksums; `doctor --global --json` reported `healthy`, with the expected warning that a custom skill destination needs explicit host discovery.

Fresh built-CLI discrepancy: `reconc hook verify --json` failed before producing its offline matrix: `bind isolated bare executable to receipt: binary version "dev+371764ff0ffb" does not match requested version "hook-verify"`. `prepareHookVerificationRepo` passes the test sentinel `hook-verify` as an installation version; managed builds now embed a real development version and provenance marker. The existing built-CLI regression used bare `go build` without that marker, so it missed the mismatch. Fix the version flow and add a managed-build regression before treating offline host verification as a final gate.

Resolution under TASK 555: the isolated offline child now passes its actual CLI version to receipt publication. A second failure surfaced after the matrix began: DSH's real-worker probe hard-linked the running executable to a disposable `.build/bin/reconc`; the next synthetic transport probe overwrote that hard link, corrupting the source build artifact and invalidating Kimi Code's receipt checksum. DSH now uses the verified streaming-copy path for its mutable worker target. The regression builds through `make build`, executes all 21 offline verification surfaces, checks DSH's real adapter result, and asserts that the managed binary SHA-256 remains unchanged. The focused test passed, and a separate `make build` plus `hook verify --json` from `/tmp` returned `complete=true` with 21/21 non-degraded results. The source binary digest was `3354e9c853706e2ef3ceb0da2f6f76048b1bb0afd0dee9fe1fac622194ddcf14` before and after. The corrupted ignored build artifact from the initial reproduction was removed and rebuilt; tracked source was unaffected. Full final gates and five-host live qualification remain pending.

The focused CLI race regression passed. `make test` completed successfully on the changed source: publication audit, generated reference/harness checks, uncached root and portable-template race suites, and isolated release trust (`release-trust: ok`, real fixture target 71 seconds). GitHub CI and CodeQL for predecessor commit `371764ff` both completed successfully; those hosted results do not cover the uncommitted TASK 555 changes. Remaining local gates and live host coverage stay separate.

The terminal confirmation helper then exposed a separate PTY bug: it entered a new process group before reading from the operator's foreground terminal, so macOS stopped it with `SIGTTIN`. The bounded reader now inherits the foreground process group, while `exec.CommandContext` still cancels it. A focused race regression and a real PTY Enter/exit probe passed after this patch; the complete gate above predates this one-line process-group change and must be repeated on final source. `make test-fast`, `make vet`, `make lint`, `make self-host`, and `make coverage` also passed before that patch (whole-module root 82.3693%, portable template 83.9867%); these are local evidence, not final hosted CI.

Fresh current-build Codex 0.154.0 live verification used Reconc SHA-256 `58ec022ef2162e1a146d4943d7dfd7e114ddc079c755a14654748a243660fb85`, an isolated repository, and the exact host executable/runtime digests recorded in the native report. The report independently completed allowed/denied write and shell, including native rejection and absent denied effects, but exited 2 with `complete=false`: read, nonzero shell, classified MCP, interrupt, subagents, and compaction remain unproven. This is a four-control qualification, not a full host pass.

Fresh Devin CLI 3000.10.21 operator-assisted probe used an isolated repository and the same Reconc build. A native `devin --print` run read the disposable `AGENTS.md`, created `allowed.txt` with the expected marker, executed the allowed shell marker, attempted both forbidden targets, and ran `exit 7`; the independent filesystem check found the two allowed effects and neither forbidden effect. The CLI's `--export` produced ATIF-v1.7 but omitted individual tool calls and outcomes, while its printed result is agent-authored prose. The Reconc verifier observed generated hook routes and exited 2 with `enforced=false`, correctly declining to bind the routes to native outcomes. Permission-request and post-compaction routes were absent. The terminal-confirmation patch removed the previous Enter hang. No full Devin enforcement claim is made from the export or prose alone.

Current OMP is 18.1.19 rather than the 18.1.18 inventory above; it requires a fresh host/config identity before qualification. Full Cursor interactive/print and OMP qualification, DSH provider-backed calls, disabled/shadowed controls, host skill discovery, and the complete real-CLI update state table remain open. The isolated DSH profile has no model credential or selection; no global DSH installation is required. TASK 556 now owns the separate checked benchmark baseline v4/v6 migration discovered from TASK 554. It does not erase TASK 554's retained 24-target paired benchmark result.

Cursor CLI print at `2026.09.10-fd3934a` ran in an isolated generated-hook repository. Its native `stream-json` had distinct started/completed tool records and typed outcomes for read success, allowed edit success, denied edit error, allowed shell success, denied shell rejection, and nonzero shell failure with exit 7. Independent effects matched: `allowed.txt` and `allowed-command-marker` existed, both forbidden markers did not. The retained local stream `/tmp/reconc-task555-cursor-print.jsonl` has SHA-256 `9f00c8a4fcd8cf1fc741d298448ad0228877d35bb2927cfb6f467f2aed6f7ba0`. The operator-assisted Reconc report still did not bind these native outcomes and one run failed with `live capture: repeated native call on the same route`. A second retained run showed the cause: Cursor emitted two `cursor-post-tool-use-failure` records with the same `tool_use_id` but different native tool names and inputs. The live-capture replay key now includes tool name and input digests; it continues to reject repeated identical tool identity. The focused race regression passed. This fixes route accounting only; native result correlation, interactive mode, MCP, compaction, controls, and full-host qualification remain open.

After rebuilding the modified source, the same Cursor print workflow again produced two failure-route records with reused `tool_use_id` and different tool identities. Operator confirmation completed without the previous replay error, and the verifier reported `loaded=true`, `observed=true`, `transport=verified`; it still correctly exited 2 with `enforced=false` because native outcomes were not imported into the operator report. The rebuilt Reconc SHA-256 was `47d7d100afe978d2cd2f68caade05fc580eefaebeaf5130ab6208345fdc464ba`.

OMP CLI 18.1.19 ran the same six operations in an isolated repository. Its native JSON stream contained six `tool_execution_start`/`tool_execution_end` pairs with `toolCallId`, tool name, arguments, `isError`, and result details; read and allowed write/shell succeeded, denied write/shell returned errors, and `exit 7` returned `isError=true` with `details.exitCode=7`. Independent filesystem effects matched the allowed and forbidden markers. The retained local stream `/tmp/reconc-task555-omp.jsonl` has SHA-256 `a72c3721658107a700b3488fd99a38e290277f300d3d596f7dc1822b`. The generated extension also logged four `reconc hook worker: live capture: unknown runtime route` errors: the verifier's capture wrapper intercepts its persistent-worker transport as if it were a runtime route. Direct route records and native outcomes show partial behavior, but the Reconc operator report remained incomplete/unbound. OMP's worker capture path must be handled before a full verifier claim; no general OMP pass is asserted from this run.

The live capture shim now passes `__worker_v1__` unchanged to the preserved generated wrapper. This prevents the verifier from turning an OMP worker handshake into a spurious runtime-route error or forcing a one-shot fallback. The persistent worker's individual events remain outside the current sanitized capture record; they must stay unproven in the operator report until worker-frame correlation is implemented. A managed-binary regression exercises a real ping/shutdown worker exchange through the shim and preserved wrapper.

The official DeepSeek Harness launcher is `npx @deepseek-ai/dsh`; a global `dsh` executable is not required. On 2026-09-13, npm `next` resolved to the already supported `0.1.5-rc.2` (while `latest` remained rc.1). An official `npx --yes @deepseek-ai/dsh@next --version` resolved and ran rc.2, and the headless `--help` boot passed in the isolated DSH home. With `--patch .dsh/reconc.patch.yml`, `--dump-config` showed `reconcGuard` on `agent-loop` and the `reconc-native` row. A headless model task exited 1 without a final answer; the isolated home has neither `$DSH_HOME/.credentials.yaml` nor `settings.yaml`, so this is not provider-backed tool proof. The published upstream CLI and provider guides separate launcher installation from model credential setup. Do not treat an absent global binary as an installation blocker or claim model enforcement from a config dump.

The project install/status/verifier guidance and README, canonical documentation, and portable skill references now give the tested pinned `npx --yes @deepseek-ai/dsh@0.1.5-rc.2` invocation. The generated patch comment retains its short `dsh --patch` spelling because the executable invoked by `npx` is named `dsh`; changing the managed artifact bytes would force needless regeneration. The live verifier still inspects a resolved local `dsh` entrypoint, so its isolated probe must put the package bin on PATH. This is a verifier identity prerequisite, not a requirement to install DSH globally. A provider-backed session remains unproven until a model credential and model selection are configured in DSH.

The exact pinned `npx` command was repeated from the isolated repository with `--dump-config` and returned `reconcGuard` on `agent-loop` plus the file-backed `reconc-native` plugin; exit 0. The real CLI update tests now exercise missing-skill suggestion and explicit installation, stale-owned skill repair with the same binary inode, and refusal to overwrite a user-modified skill through `update check` and `update apply`. Focused usercli tests and the DSH hook/managed-verifier tests passed. These tests establish those CLI state transitions, not host skill loading or a provider-backed DSH decision.

The official npm package can also supply the verifier's local host identity without a global install: `npm exec --yes --package=@deepseek-ai/dsh@0.1.5-rc.2 -- reconc hook verify --live --host dsh --surface cli --allow-authenticated --json` resolved the package's `dsh` entrypoint, verified version `0.1.5-rc.2` and SHA-256 `0ff7f1d72c4e0cbe14001709c81e20a04b70464118a7f78568952988e28f2ac5`, and reported `host_available=true`. It exited 2 with `complete=false` and all native operations unproven because JSON mode deliberately did not launch the host. This is the correct non-authenticated discovery result, not a failed installation.

After the DSH action text changed, `make test-fast` first identified the expected generated hook-reference drift in `docs/architecture.md`. The single generated DSH row was synchronized surgically and `make reference-docs-check` passed. The repeated complete `make test-fast` then passed both root and portable-template modules. `make vet`, `make lint`, the skill validator, and `git diff --check` also passed. Full uncached/race `make test`, self-host, coverage, live host controls, and hosted CI have not been rerun on this final uncommitted source; task acceptance remains open.

Final scope correction: the user explicitly declined another real DSH run or any further agent-host test run. The official `dsh-v0.1.5-rc.2` source at `fb2c4b9e698e30edb738bca4cf0618587db7d203` was re-read for `apps/cli/src/args.ts`, `apps/cli/src/profile-boot.ts`, `packages/core/tools/src/index.ts`, and `packages/core/agent/src/runtime-types.ts`. The pinned ToolRuntime source matches the retained initial source byte-for-byte (SHA-256 `cc8d54b6a0d6005a26871e84ce3390ba0bbd7fa61a8c65d4b9cc2fbd86126ac2`). Its signatures confirm the asynchronous pre-execute waterfall, synchronous monotonic guard before dispatch, frozen final result observation, awaited pre-step decision, and serial turn-stopping steering. The CLI source confirms that `--patch` is accepted with `--profile` and applied after the bundle, profile, and home layers; it resolves the patch path from the invocation directory. Generated `.dsh/reconc.patch.yml` inserts the extension and makes `agent-loop` depend on `reconcGuard`. The previously retained published-runtime probe in TASK 550 confirms loading, allow/deny, skipped pre-listener denial, and lifecycle on the pinned package. This review found no new mismatch requiring a DSH code change. It is source and offline-runtime confidence, not a claim about a provider-backed model call.

| Host/mode and pinned observation | Proven operations | Still unproven in this task |
| --- | --- | --- |
| Codex CLI 0.154.0 | Native allowed/denied write and shell, denied effects absent; exact executable/runtime identity retained | Read, nonzero shell, classified MCP, interruption, compaction, subagents |
| Devin CLI 3000.10.21 print | Allowed write/shell effects present and forbidden effects absent in an isolated repository | Native typed outcome correlation, permission and compaction routes; prose/ATIF does not prove enforcement |
| Cursor Agent CLI 2026.09.10-fd3934a print; interactive route inventory in TASK 548 | Native typed read, edit, shell, denial, and nonzero-shell outcomes with matching independent effects in print mode; reused call ID across different tools now accepted by capture | Reconc operator-result correlation, interactive enforcement, MCP, compaction, subagents |
| OMP CLI 18.1.19 | Native six-operation JSON stream and independent effects; offline managed-binary worker-shim handshake fixed | Persistent-worker event correlation in the operator report, broader host and MCP controls |
| DSH `dsh-v0.1.5-rc.2` | Pinned source/profile API, generated extension, published ToolRuntime offline allow/deny and bypass guard, synthetic real-Go-worker adapter matrix | Provider-backed model tool call, full subagent lifecycle, native result/Stop observation |

The generated adapters and optional skill remain separate from host enforcement. No full five-host live pass is claimed; unfinished correlation in the optional live verifier stays visible instead of being inferred from native streams or filesystem effects.

Final local gates on this source passed: `make test-fast`, `make test` (uncached root and portable race suites, publication audit, reference docs, harness pack, release trust), `make vet`, `make lint`, `make build`, `make self-host`, `make coverage` (whole-module root 82.3729%, portable template 83.9867%), skill validation, and `git diff --check`. The predecessor's hosted CI/CodeQL result is not reused as this task's result; exact-commit hosted status is checked after push. No DSH or other agent host was launched in this final pass.

## Deviations

The original plan called for new live host controls, including provider-backed DSH execution. The user explicitly ruled out a real DSH run and any further agent test run for this completion pass. Source/API review and offline adapter tests replace that execution requirement; native DSH enforcement and unobserved routes remain unqualified.
