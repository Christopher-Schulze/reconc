# TASK 547: Close Devin tool and session gaps

## Why

The generated Devin pre/permission matcher is limited to exec and edit. The current documented tool surface includes additional file mutations and MCP calls. Session normalization also falls back to a repository-derived shared identity despite current documented per-session and per-turn identifiers.

## Acceptance

- Every currently supported Devin file mutation and shell/MCP entrypoint is either classified and gated or explicitly reported unsupported.
- Two concurrent Devin sessions in one repository cannot share policy evidence through a repository-only identity fallback.
- Actual successful, failed, canceled, denied, and rewritten operations retain truthful call/turn binding.
- Native and compatibility-loaded hooks do not double-count events.
- Version-qualified tests and live results distinguish host timeout, malformed output, and co-hook input rewriting from Reconc's own failure handling.

## Sub-Tasks

- [x] Record current event/tool payloads and compare generator, normalizer, and MCP classification.
- [x] Close matcher/normalization gaps and correct session/turn identity handling.
- [x] Add concurrent-session, process-control, rewrite, and response-shape regressions.
- [x] Update fixtures, docs, and live qualification for the verified Devin version.

## Technical Plan

1. Change the Devin generator in `internal/hooks/platform_generators.go`, registry entries in `platforms.go`, `internal/runtime/agentsession/devin.go`, existing Devin tests, and the namespaced MCP owner only where needed.
2. The official lifecycle list includes `write`, `apply_patch`, `notebook_edit`, process interaction, `mcp_call_tool`, and namespaced MCP tools beyond the current pre matcher. Build a typed normalization table from observed payloads. For process interaction, bind stdin and completion to the original execution; never blindly mark a polling call successful command evidence.
3. Extend pre/permission/post matchers consistently. Normalize direct file mutation arguments through existing path/write accounting. Route native MCP dispatch envelopes and namespaced tools through `mcp_payload.go` and `namespaced_mcp.go`; do not confuse MCP management or resource reads with downstream writes.
4. Preserve native `session_id` and bind `prompt_id` where present. Missing identity on a policy-relevant event must produce an explicit degraded/refused state rather than silently merging unrelated sessions. If older hosts remain supported, constrain their fallback to a separately proven session identity and document that compatibility boundary.
5. Validate event-name/CWD checks, forged envelope rejection, input rewriting order, and authoritative post-result fields. A successful hook process or textual output is not a successful tool execution.
6. Test the host's handling of exit codes, malformed output, timeouts, and duplicate Claude-compatible configuration. Retain bounded Stop follow-ups and reinject context after compaction without replaying evidence.

## Verification

Run `go test ./internal/hooks ./internal/runtime/agentsession ./internal/cli`. Required isolated scenarios: concurrent sessions, first prompt without earlier prompt_id, changed prompt_id, missing session ID, denied direct write/apply_patch/notebook edit, failed shell, process input/completion, direct/namespaced MCP, duplicate compatibility hooks, and conflicting rewritten inputs. Live verification uses the actual Devin executable with TASK 545 receipts.

## Dependencies

TASK 545. Coordinate shared MCP changes with TASKS 546 and 550 and context behavior with TASK 553.

## Sources and Evidence

- [Devin lifecycle, identity, and tool inventory](https://docs.devin.ai/cli/extensibility/hooks/lifecycle-hooks).
- [Devin hook configuration and output contract](https://docs.devin.ai/cli/extensibility/hooks/overview).
- Checked 2026-09-12; installed binary reports `3000.10.21 (611c1cba)`. Current source observations: `^(exec|edit)$` pre/permission matcher and repository-SHA fallback in `NormalizeDevinPayload`. Native identity requirements must be qualified against that exact binary, not inferred only from documentation.

## Notes

The matcher omission and shared fallback exist in source. Whether every documented newer tool is exposed by the installed model/mode remains a live qualification item.

Execution-entry check after TASK 546: `origin/main` and local HEAD both identify `74f43db24e5cdc5cacd2141db707b747a71e8090`, and the working tree was clean before activating this task. CodeQL passed for that commit; GitHub CI remains in the Linux race job and must not be called green yet. The installed `devin --version` reports `devin 3000.10.21 (611c1cba)`. The official lifecycle and hook configuration pages were refreshed on 2026-09-13. They specify stable per-session `session_id`, per-turn `prompt_id` absent before the first prompt, case-sensitive event names, regex tool matchers, a `tool_response` with `success`/`output`/`error`, and distinct exit-2 block versus other-error behavior. The tool inventory includes `write`, `apply_patch`, `notebook_edit`, `get_output`, `write_to_process`, `kill_shell`, `mcp_call_tool`, and namespaced `mcp__` tools beyond Reconc's current `^(exec|edit)$` pre/permission matcher. The live tool set may vary by CLI mode/model/integrations. No repository-targeted Devin invocation has occurred in this task.

The prior CI run subsequently completed green for exact commit `74f43db24e5cdc5cacd2141db707b747a71e8090`: CI `34724914467` passed Linux/macOS, release trust, and LangChain MCP; CodeQL `34724914447` passed. Windows was intentionally skipped. The installed Devin CLI initially rejected noninteractive execution in an untrusted disposable repository before any hook fired. Its own `--help` documents `--respect-workspace-trust false` for this isolated mode. A repeat with that command-scoped flag exited zero and captured six actual host events: SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, Stop, SessionEnd. Sanitized receipt `.build/task547/native-devin-payload-shape-trust-override.json` contains only field names, tool-input/result keys, boolean success, and SHA-256 identities. SessionStart had `session_id` and no `prompt_id`; subsequent events shared the same session and prompt digests. The actual `exec` pre/post events had the same nonempty `tool_use_id`, input key `command`, and post response keys `success`, `output`, `error` with success true. No `cwd` field appeared in these native events. This is one shell operation, not evidence for the untested file/process/MCP tool shapes.

Native file-mutation control on the same installed CLI used a disposable repository, `--permission-mode accept-edits`, and command-scoped workspace trust. It exited zero and left the expected `probe.txt` content `beta` after actual `write`, `read`, and `edit` tool calls; sanitized receipt `.build/task547/native-devin-write-shapes.json` has the exact event sequence and identity digests. `write` used `file_path` and `content`, `read` used `file_path`, and `edit` used `file_path`, `old_string`, and `new_string`. Every pre/post pair carried the same nonempty `tool_use_id`, session and prompt identities; post response had `success`, `output`, and `error`. The current generated `^(exec|edit)$` pre/permission matcher therefore misses a native write that really executed. The prompt requested `apply_patch`, but the model selected `edit`; no native `apply_patch` shape or enforcement is claimed from this run.

Shell-outcome negative control: under `--permission-mode auto`, a requested `exit 7` yielded only PreToolUse and no post event, so permission refusal could not be separated from execution. With command-scoped `--permission-mode dangerous` in a disposable repository, the installed host delivered both events. A final exact-input control confirmed that `tool_input.command` was byte-for-byte `exit 7`, with identical pre/post `tool_use_id`; `PostToolUse.tool_response` contained `success=true`, `output` mentioning exit code 7, and `error` empty, but no structured exit status. Sanitized receipts: `.build/task547/native-devin-nonzero-shape.json`, `native-devin-nonzero-dangerous.json`, and `native-devin-exit-seven-exact.json`. Reconc's present `HookHandlerPostToolUseComplete` would incorrectly promote this callback to successful command evidence. The repair must treat native `success` as tool-call completion only, require an authoritative structured exit for direct shell-success evidence, and leave the output text non-authoritative. A separate `reconc exec` path can still prove actual command success.

Implementation design from the current code path: replace the repository-SHA/alias/env fallback in `devinSessionID` with exact native `session_id` validation; the current installed host supplies it even on SessionStart. Preserve `prompt_id` and, for tool events, bind its stable turn value to the observed `tool_use_id` so a host that reuses a short call ID in a later turn cannot replay an earlier decision. Keep SessionStart's absent `prompt_id` valid. Reject duplicate JSON keys before normalization. Map documented mutation names to existing write accounting, including native `write`/`edit`; `apply_patch` and notebook mutation without an extractable path must fail closed. Widen generated pre/permission/post matchers consistently, but keep process-input and direct MCP dispatch blocked with an explicit unsupported reason until their exact payload/correlation shape is observed. Namespaced MCP calls can use the existing exact selector/effect model after Devin becomes a built-in MCP platform. Never classify MCP management or resource reads as downstream writes. Add a Devin-specific post observer that records native file outcomes but does not certify direct shell success from `success=true` or text; explicit structured exit metadata may be evaluated strictly if present. Finish with isolated native controls and cross-platform regressions, then update the fixture, docs, generated scaffold/pack, and final gate evidence.

Current implementation checkpoint: Native Devin tool events now require exact `session_id`, `prompt_id`, `tool_use_id`, and `tool_name`; SessionStart may lack `prompt_id`. The observed absence of payload `cwd` is accepted only when canonical `DEVIN_PROJECT_DIR` equals the target repository. The pre/permission/post matchers capture documented file, process, shell, and MCP tools. Native `write`, `edit`, notebook and parseable patch writes reach the real policy gate. Process stdin, direct MCP dispatch, namespaced MCP, and matched unclassified names are explicitly blocked because their host envelopes remain unverified; read-only process polling and cancellation remain passive. The pre hook persists only a digest of the permitted tool input under the session and turn-bound call ID. Post events consume that correlation once; another session/turn, rewritten input, post-only callback, failed result, or duplicate post callback cannot create successful write evidence. The existing Antigravity retired-key cap is not reused for Devin, so long sessions do not exhaust that unrelated replay generation. Exact `exit 7` with native `success=true` but no structured exit stays passive; a structured, internally consistent exit can supply command evidence. Synthetic package tests pass for these boundaries. The complete package run exposed three stale synthetic fixtures and the old checked-in scaffold matcher; each was updated to the newly observed native shape, and the formerly failing focused checks now pass.

Native installed-host control on `devin 3000.10.21 (611c1cba)` used a disposable Git repository, a policy compiled by the current development binary, the generator-identical checked-in `.devin/hooks.v1.json`, and a disposable wrapper pointing to that binary. With command-scoped `--respect-workspace-trust false` and `--permission-mode accept-edits`, Devin exited zero, surfaced the policy denial, and did not create `generated/blocked.go`. `reconc hook status --json` in that disposable repository showed SessionStart, UserPromptSubmit, PreToolUse, Stop, and SessionEnd liveness. This proves one live native write denial through the new matcher, not every documented tool. A first attempt to use `reconc init` with a private `RECONC_HOME` was refused by its global installation receipt; a second with a development binary placed before the installed binary on PATH was also refused. The final disposable test avoided both global-ownership changes and product-repository Reconc commands.

Additional installed-host controls used only disposable Git repositories. A `PreToolUse` `write` hook sleeping three seconds under a one-second host timeout produced a timeout diagnostic and let the requested file be written unchanged. A hook emitting malformed JSON with exit zero produced a parse diagnostic and also let the write execute. The official [output contract](https://docs.devin.ai/cli/extensibility/hooks/overview) requires `hookSpecificOutput` tagged `PreToolUse` for `updatedInput`; with that exact shape, a native rewrite changed both `file_path` and `content`, and only the rewritten file was created. A two-hook chain then proved that a first hook inspecting and permitting `allowed.txt` can be followed by a second hook rewriting it to `blocked.txt`; the installed host wrote `blocked.txt`. These controls qualify Devin host behavior, not a Reconc regression. Reconc can refuse post evidence for a changed input, but it cannot make the host's timeout or a later co-hook rewrite fail closed before the tool runs. The user documentation now states that limitation and points to the separate git-derived checkpoint and pre-commit gate. Native tool envelopes for `apply_patch`, notebook edits, process stdin, and MCP dispatch remain unobserved; their preflight classification is synthetic and process/MCP writes are explicitly blocked when matched.

An independent code review found that copying top-level path or command aliases into Devin's native `tool_input` could change which arguments Reconc gated. Normalization now requires the exact native `tool_input` object on all tool events and never overlays top-level aliases. A new concurrent two-session regression executes preflights simultaneously with identical turn and call strings; only the permitted session can record the subsequent write. Process polling/cancellation remains passive and cannot invent repository evidence. Focused Devin and generated-scaffold tests pass after these changes.

A final native rewrite control captured both hook envelopes in a disposable repository: `PreToolUse` received the absolute original path and original content, while `PostToolUse` carried the rewritten path and content under the same `tool_use_id`; only the rewritten file existed. This confirms the exact input mismatch that Reconc's post correlation rejects. It does not make pre-execution enforcement safe against a later co-hook rewrite. `make test-fast`, `make test` including both uncached race modules, `make vet`, `make lint`, and isolated `make self-host` passed. Publication audit and deterministic pack check passed after the checked-in derived pack was refreshed. The `.build/task547` snapshots named above were transient and were removed by the release-trust test's build cleanup; this task record retains the observed shapes and qualification limits, not a claim that those ignored files remain available. No product release, tag, global install, or Windows execution occurred.

## Deviations

The checked-in binary harness pack cannot be edited as source text. A separate candidate was generated and compared before the standard atomic pack writer updated the existing derived manifest and archive. All 120 archive entries were retained; only the manifest and the two changed source entries differ. This scoped regeneration was required by the publication audit and is an exception to the normal no-regeneration rule for existing files.
