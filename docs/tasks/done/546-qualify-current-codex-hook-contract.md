# TASK 546: Qualify current Codex hook contract

## Why

The Codex adapter is pinned to an older source contract. Current documentation includes Interrupt and a broader local-function hook surface than the generated matchers cover. Unsupported response fields can also change host behavior.

## Acceptance

- An explicit version-qualified event/tool inventory accounts for current Codex CLI behavior and the existing compatibility baseline.
- Supported policy-relevant tool routes are normalized and gated; excluded hosted/transport routes are named honestly.
- Interrupt, permissions, async command completion, compaction, and subagent behavior have real behavioral tests and bounded lifecycle handling.
- Generated configuration is discoverable under the tested trust/config layers, and live enforcement is reported only with TASK 545 evidence.
- Existing Codex behavior and other platforms retain their guarantees.

## Sub-Tasks

- [x] Pin the actual supported Codex source/version and compare its event signatures with the registry and generated configuration.
- [x] Implement the verified event, matcher, response, and lifecycle changes with compatible normalization.
- [x] Add generated-config and runtime regressions, including deny-response negative controls.
- [x] Update versioned fixtures and integration docs; qualify the CLI using TASK 545.

## Technical Plan

1. Read `internal/hooks/platforms.go`, the Codex generator in `generators.go`, `codex_activation*.go`, `internal/runtime/agentsession` normalization/permission/compaction handlers, and `internal/hooks/testdata/host-events/codex.json` together. Keep the registry as the capability owner.
2. Compare installed `codex-cli 0.154.0` with the pinned source that actually produced that build. Current upstream HEAD is not automatically its contract. The existing event fixture references `45f8cafa4e2ec20f9b189d5aa9409e424b6d3d09`.
3. Current official hooks documentation exposes Interrupt with a short deadline and no veto/restart semantics; most local function tools now participate in pre/post hooks. Inventory exact tool names and aliases before widening `Write|Edit|MultiEdit|Bash|apply_patch`. Handle code-mode inner calls and asynchronous execution completion without counting polling as a second execution.
4. Route Interrupt to bounded cancellation/lifecycle observation, never completion approval. Keep SessionEnd cleanup separate. Verify compaction reinjection and child-session identity against TASK 553.
5. Emit only response fields accepted for the particular event: valid PreToolUse denial, nested PermissionRequest decisions, and the supported context shape. Test fields the host rejects rather than assuming Claude compatibility. Ensure MCP permission/pre routes use the existing classified MCP policy model.
6. Verify `.codex/hooks.json` plus `.codex/config.toml` activation against layered config, trusted/untrusted repositories, and managed-only hooks. Report configuration suppression as unavailable, not loaded.
7. Propagate generated artifacts, status, schemas, host fixture, `docs/documentation.md`, `internal/agentguide/guide.md`, and the portable platform reference. Preserve the CLI/Git/CI fallback and explicitly identify hosted tools that lack hooks.

## Verification

Run `go test ./internal/hooks ./internal/runtime/agentsession ./internal/cli` and the existing generated-hook integration tests. Exercise valid/malformed deny JSON, nonzero execution, blocked edit/apply_patch, supported local-function variants, MCP denial, repeated polling, cancellation, compaction, and child cleanup. The final live matrix must contain the exact Codex executable and tested modes; an upstream document alone is not a passed test.

## Dependencies

TASK 545. Coordinate briefing reinjection with TASK 553; TASK 555 owns final combined qualification.

## Sources and Evidence

- [Official Codex hook contract](https://learn.chatgpt.com/docs/hooks), checked 2026-09-12.
- [Existing pinned source contract](https://github.com/openai/codex/tree/45f8cafa4e2ec20f9b189d5aa9409e424b6d3d09/codex-rs).
- Local generated matcher and registry inspection at source `25ce22668769dbbb8c2360f8db6e7ecc574033e1`. Interrupt is absent from the current registry. Wider matcher needs are candidates until actual tool payloads are verified.

## Notes

Execution-entry reality check: TASK 545 is committed and pushed as `cbd70b2227c37397fc862db955bd6979e1bf714e`; local full race, fast, lint, vet, self-host, and staged publication checks passed. Native Codex demonstrated four independent write/shell controls; remaining operations are explicitly unproven. GitHub's annotated `rust-v0.154.0` tag resolves through tag object `36eab01061df3cde5f95ec20a526777b430091ba` to commit `6b9826e3aa83b1a5947db50f4332cb9c65f1b340`. Use that exact source for the installed host contract. The registry has a shared Interrupt event but no Codex capability or generated binding; its fixture still describes eleven events from the older commit.

No Codex global configuration or active task was changed during planning. Do not broaden this task into desktop/cloud product integrations beyond preserving existing behavior.

Pinned-source comparison (local source cache `.build/task546/upstream`, official document `.build/task546/official-hooks.md`):

| Boundary | Verified source behavior | Required implementation/control |
| --- | --- | --- |
| Interrupt | `hooks/src/events/interrupt.rs` returns lifecycle observations only; output accepts empty or JSON warning; official timeout is 1-3 seconds | Add the missing Codex capability and generated command with a bounded advisory path. Never route it through Stop approval or reset durable run mode. |
| Local tools | `core/src/tools/registry.rs` supplies default pre/post payloads for function tools; `hook_names.rs` keeps canonical `apply_patch` with `Write`/`Edit` matcher aliases and canonical `spawn_agent` with `Agent` alias | Replace the narrow generated matcher with coverage of the actual local-function path while preserving classified MCP routing and avoiding duplicate deliveries. Hosted and explicitly opted-out transports remain outside the claim. |
| Async shell | `handlers/unified_exec/write_stdin.rs` emits no new pre-hook; its terminal post payload belongs to the original command | Bind the original command/call and explicit terminal outcome; test repeated polling without duplicate evidence or invented success. |
| Permission | `events/permission_request.rs` consumes nested `decision.behavior` and requires a reason for exit-2 denial | Preserve the existing nested Reconc denial shape; cover classified MCP approval and malformed/unsupported response fields against the pinned contract. |
| Compaction | `events/compact.rs:279` records `systemMessage` as a warning and exposes no additional-context result; Reconc's `AdaptCodexCompactionResult` currently emits only that field | A warning is not model-context reinjection. Use the supported `SessionStart` compact source for recovery, retain accumulated session evidence, and coordinate packet deduplication with TASK 553. |

These are source-backed implementation requirements, not live passes for the remaining matrix. TASK 545 hosted CI run `34720883488` and CodeQL run `34720883482` were still running at this checkpoint; LangChain interoperability had passed and Windows smoke was intentionally skipped.

The lifecycle implementation now generates advisory Interrupt (3 seconds) and a separate compact SessionStart recovery route. Focused real CLI and generator controls passed; recovery retains read evidence and emits model-visible additionalContext, while malformed or foreign lifecycle envelopes cannot inject context. TASK 545 CI `34720883488` and CodeQL `34720883482` both completed successfully for exact commit `cbd70b2227c37397fc862db955bd6979e1bf714e`; Windows remained intentionally skipped.

Further pinned-source finding: `core/src/tools/context.rs:404-413` returns raw output text for terminal unified-exec PostToolUse, not the exit code exposed separately in the model-facing result. Nonterminal processes produce no post payload, and terminal polling retains the original command/call ID. The generic completion handler therefore incorrectly promoted output-only native callbacks to successful command evidence. A Codex-specific observer now leaves that outcome unproven, preserves compatible explicit exits with strict consistency checks, and treats interruption as failure. Product documentation no longer claims native shell outcome coverage from output text. Remaining matcher, MCP permission, subagent, live qualification, and complete task gates are still open.

Matcher/permission checkpoint: the generated pre/post groups now partition all nonempty local names from the reserved MCP prefix using syntax supported by the pinned Rust regex parser. The test corpus checks both generated groups against an independent prefix oracle, including all names of length 1-6 over the namespace alphabet and the canonical/compatibility tool names. PermissionRequest now matches all tools and applies classified MCP policy with nested native deny output; allowed requests leave the host prompt intact. Focused CLI/generator tests passed in `.build/task546/codex-matcher-permission.log`. Existing matcher assertions were updated because the requested current local-function contract supersedes their narrow allowlist, and behavioral partition tests now own coverage.

Native checkpoint after these changes: `.build/task546/native-codex-checkpoint.json` binds installed Codex CLI 0.154.0 to runtime digest `4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`, generated configuration digest, original process capture, and independent marker checks. All four allowed/denied write/shell controls passed; the owned native process exited 0. The CLI correctly reports overall `complete=false` because read, nonzero-shell, classified-MCP, and remaining lifecycle qualification are still unproven. `go run` therefore exits 1 while reporting the verifier's incomplete exit status in stderr; this is not a failed four-control probe.

Subagent source boundary: `core/src/hook_runtime.rs:383-473` selects SubagentStop for thread-spawned child turns. The initial inference that sess.session_id() identifies that child was incorrect: `core/src/session/session.rs:598-604` proves session_id is shared by the root and all descendants, while thread_id supplies the child's agent_id. `hooks/src/schema.rs` carries agent_id on child lifecycle and local/MCP/permission tool envelopes. SessionEnd is root-only; internal/synthetic subagents expose no user lifecycle hooks. Child policy checks must normalize agent_id before any namespaced MCP extraction, never run the parent's durable TASK continuation, and never erase evidence on every child turn. The live report's duration_ms is the maximum observed hook duration (`applyLiveHookProbeRecords`), not total native-run duration; the receipt separately retains native start/end timestamps.

MCP attribution repair: the shared namespaced normalizer now preserves a supplied valid tool_use_id and rejects duplicate JSON keys and malformed identities. Real persisted evidence tests prove distinct calls with identical arguments advance the write epoch independently, while immediate duplicate delivery of the same call does not. Both Codex and Claude Code controls passed, including race checks (`mcp-identity-controls.log`, `mcp-identity-race.log`). Namespaced normalization errors are preserved through Codex lifecycle dispatch. The first full fast gate found two propagation gaps: a historical NotebookEdit matcher expectation and the portable workflow audit's missing new route names. Both were corrected against the actual contract; dedicated propagation checks passed, and the complete fast gate is rerunning.

Checkpoint verification: focused lifecycle/outcome/matcher/permission race controls passed (`codex-controls-race.log`), followed by MCP identity race controls and the portable route/scaffold parity checks. Updated source passes lint, vet, and generated-reference validation (`lint-propagated.log`, `vet-propagated.log`, `reference-propagated.log`). The deterministic harness manifest/archive were refreshed after reviewing exactly two changed source entries (workflow audit and Codex hook scaffold); pack validation passes with digest `31ea11306aded0674848bc5a1f1c4bb357617eb9055e0bab2a7eeda018576ccd`. All 25 private historical task files retain their original hashes. Full fast rerun, final full gates, subagent behavior, and expanded native qualification remain required before archival or commit.

Child lifecycle implementation checkpoint: Codex normalization now separates agent_id before local, permission, and namespaced MCP routing, preserves opaque JSON arguments, and rejects malformed or missing native child identity. SubagentStop runs the child's policy check with a 30-second host deadline, fail-closed operational errors, and the native stop_hook_active recursion bound. Unresolved repeated turns, explicit interruption, or evidence overflow are persisted as uncertified; later turns check again. Clean checks retain evidence and leave repository TASK continuation and parent state untouched. Two sibling children sharing a native parent session are exercised through the real CLI and compiled MCP policy. Focused Codex/MCP/routing/scaffold race tests passed (`.build/task546/codex-child-race.log`: CLI 4.805s, agentsession 2.259s, hooks 2.050s). The earlier full fast run completed successfully. The first child identity negative-control assertion incorrectly expected a returned CLIError to be written to the test helper's stderr; the corrected test checks both the actual returned diagnostic and directly emitted stderr, preserving the exit-2/empty-stdout requirement. Expanded native lifecycle/trust qualification and final full gates remain open; no TASK 546 commit or push is claimed.

Further verification: the final child implementation passes `make test-fast` across root and portable modules (`.build/task546/test-fast-child-verified.log`, CLI 102.854s and agentsession 101.443s). Final lint, vet, generated-reference checks, and deterministic pack validation pass; the pack digest is `e35bc5c31c0a658a426e5d123d691ff9c3ad71016e9de7b4e4ee5e7a5a230ab0`. All 25 private historical task files remain byte-identical. The overflow regression now drives the real bounded-command append path, verifies the retained segment head through the CLI, and reloads the complete evidence chain inside the owning package. Setting a bare overflow flag was an invalid test setup, and reading only the hot snapshot omitted correctly rotated evidence; the final chain check uses the resolved repository identity required by the internal reader and passes under race detection.

Next native qualification boundary: `executeCodexLiveProbe` currently uses `--dangerously-bypass-hook-trust` plus command-scoped project trust. Its four successful controls prove native dispatch/enforcement under that explicit invocation, not ordinary persisted hook trust or every configuration layer. Pinned `hooks/src/config_rules.rs:8-64` accepts per-hook state only from user/session-flag layers. `engine/discovery.rs:664-719,775-821` binds key plus normalized definition hash, skips untrusted/modified/disabled non-managed hooks, and supports managed-only filtering. The next implementation/control pass must qualify this normal trust path and its suppression controls without editing the user's global configuration, then extend the remaining live operation/lifecycle matrix. Cached exact-source files now include discovery.rs, output_parser.rs, and config/src/hook_config.rs. No native child or normal-trust proof is claimed yet.

Final focused race checkpoint: `.build/task546/codex-child-final-controls.log` passes every selected Codex, namespaced MCP, normalized routing, and scaffold parity control across CLI (4.917s), agentsession (2.235s), and hooks (1.754s), including the corrected complete-chain overflow regression. All test and verification processes from this implementation checkpoint are terminal; the next step is native trust qualification, not restarting a still-running gate.

Native trust qualification: the installed `codex-cli 0.154.0` app-server `hooks/list` loaded all 15 generated project hook entries from an isolated repository and temporary `CODEX_HOME`, with no errors or warnings. Its executable SHA-256 is `4f85982624b3898c8991cb80c0981b2aa71070e3537046c9a95950318a95afcc`; the generated artifact SHA-256 is `f42a108e4c9704a44a95af1fcb49f3bf38ab107be6b2a894bcc09ef9c56b3626`. An otherwise trusted project reports the selected `PreToolUse` handler as `untrusted`; exact user-layer definition-hash trust changes it to `trusted`, a wrong hash to `modified`, and exact trust plus `enabled = false` retains `trusted` but disables it. The selected normalized hash is `sha256:cb99da4f1b2dd54770219bc87e78903fa9dd89e0658a032315d306f7a9f89777`. Machine-readable local receipt: `.build/task546/codex-trust-control.json`. This proves discovery and suppression state, not normal-trust action enforcement. Managed-only suppression is sourced from the pinned hook discovery and requirements-layer implementation; the installed host's system/managed requirements were not changed. Configured `reconc hook status` now explicitly says project trust, exact hook trust, and managed-only restrictions are unverified, with a real generated-install regression and corresponding docs.

Post-trust-checkpoint gates: `make test-fast` passed for both modules (`.build/task546/test-fast-trust-status.log`). `make test` passed the complete uncached race suites, publication audit, generated references, deterministic pack, and release trust (`.build/task546/full-test-trust-status.log`, release target 89 seconds). `make vet`, `make lint`, and isolated `make self-host` passed (`.build/task546/vet-trust-status.log`, `lint-trust-status.log`, `self-host-trust-status.log`). The portable archive digest remains `e35bc5c31c0a658a426e5d123d691ff9c3ad71016e9de7b4e4ee5e7a5a230ab0`. The live verifier documentation now names its explicit one-off hook-trust bypass and distinguishes its four native controls from ordinary persisted trust and untested routes.

Project-trust negative control: the same generated artifact under installed Codex CLI 0.154.0 produced zero discovered project hooks in an isolated repository without project trust; `.build/task546/codex-project-trust-control.json` records the executable and artifact digests. The final documentation-only correction passed `make reference-docs-check`, `make publication-audit`, and `git diff --check`. TASK 546 qualifies the pinned source contract, generated/runtime regressions, native discovery and trust-state controls, and TASK 545's four native allow/deny operations under its explicitly bypassed one-off trust mode. It does not certify normal-trust tool enforcement, administrative managed-only behavior on an unconfigured host, or the remaining read/nonzero/MCP/lifecycle native modes; TASK 555 retains the combined native delivery matrix. Those routes are not presented as enforced by this task.

## Deviations

None.
